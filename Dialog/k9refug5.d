BEGIN k9refug5
IF ~RandomNum(3,1)~ THEN BEGIN 0
SAY ~How are we supposed to pick ourselves up after all this? It will be a gods amount of work just to get the farm repaired alone..~ IF ~~ EXIT
END

IF ~RandomNum(3,2)~ THEN BEGIN 0
SAY ~Just a bunch of ingrateful curs!~ IF ~~ EXIT
END

IF ~RandomNum(3,3)~ THEN BEGIN 0
SAY ~I wonder if Daggerford will have us?~ IF ~~ EXIT
END
