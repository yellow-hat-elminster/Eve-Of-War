BEGIN k9bently



IF ~OR(2) NumTimesTalkedTo(0) !NumTimesTalkedTo(0) ~ THEN

BEGIN deadtalk

SAY ~(Bentley Mirrorshade doesn't even seem to recognize that you are trying to talk to him.)~  IF ~~ EXIT

END
